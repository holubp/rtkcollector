# Verification Matrix

| Requirement ID | Verification type | Evidence | Status | Notes |
| --- | --- | --- | --- | --- |
| `ARCH-RAW-001` | Automated + manual | Session writer tests; replay comparison | Needs review | Confirm current tests cover byte-for-byte replay. |
| `ARCH-RAW-002` | Automated + manual | Parser-failure isolation tests; malformed stream session | Needs review | Confirm parser exceptions cannot stop raw capture. |
| `ARCH-RAW-003` | Automated + manual | `RawRecordingDurabilityTest`; `RecordingForegroundService` writer-open/reconnect/maintenance failure-path review; storage-full/provider smoke test | Implemented, not field-tested | Writer-open and raw append/flush failures are typed as `STORAGE/FATAL` and never USB retry; storage-full/provider behaviour still needs field validation. |
| `ARCH-RAW-004` | Automated + manual | `RawRecordingDurabilityTest`; `SessionWritersTest`; service startup/reconnect/steady-loop flush review; process-termination smoke test | Implemented, not field-tested | Receiver RX has an independent bounded byte/time flush policy, and a USB read failure forces a raw-only flush before reconnect; process-termination durability still needs device validation. |
| `ARCH-TX-001` | Automated + review | TX artifact tests; serial write path review | Needs review | Include correction injection and command scripts. |
| `ARCH-CORR-001` | Automated + manual | NTRIP runtime tests; rover+NTRIP session | Needs review | Verify `correction-input.raw` is usable RTCM3. |
| `ARCH-WRITER-001` | Automated + review | `SessionWritersTest`; `PathRecordingSessionWritersTest`; `AdvisoryFanoutTest`; path/SAF close and service-stop review | Implemented, not field-tested | Every opened stream receives an independent finalisation attempt with per-artifact issue attribution, and writer close waits for advisory access to terminate; provider-specific close behaviour remains untested on Android. |
| `ARCH-WRITER-002` | Automated + review | `AdvisoryFanoutTest`; `NtripCasterUploadControllerTest`; `RtklibWorkerTest`; existing writer close tests; service stop-order review | Implemented, not field-tested | Shutdown closes active and reconnecting USB transports, blocks a new start, and waits for confirmed capture, persistent-command, advisory, RTKLIB and caster-upload termination before closing writers; Android lifecycle and provider timing remain untested. |
| `CORR-UPLOAD-001` | Automated + manual | `NtripCasterUploadControllerTest`; base-caster replay validation | Implemented, not field-tested | Confirm only valid extracted RTCM3 frames are uploaded; OBSVM/OBSVMCMPB/BESTSAT/NMEA are not offered to upload queue/controller. |
| `CORR-UPLOAD-002` | Automated + review | `NtripCasterUploadControllerTest`; service call-site review | Implemented, not field-tested | Confirm upload consumes only post-capture RTCM queue data and upload failure does not stop or mutate receiver raw capture. |
| `CORR-UPLOAD-003` | Automated + manual | `DashboardServiceMapperTest`; `DashboardStateTest`; `NtripCasterUploadControllerTest`; service state review | Implemented, not field-tested | Confirm upload monitor fields are visible before and during recording, including bytes, bitrate, total RTCM Hz and per-message rates. |
| `CORR-UPLOAD-004` | Automated + manual | `CasterUploadProfileTest`; `NtripCasterUploadControllerTest`; service policy mapping review | Implemented, not field-tested | Validate fixed/adaptive retry modes, retry limits, and minimum 10s fixed and initial adaptive delay. |
| `CORR-UPLOAD-005` | Automated + manual | `NtripCasterUploadControllerTest`; service runtime review | Implemented, not field-tested | Validate retry limit, no-data watchdog, bitrate cap and per-session volume safety stops surface explicit terminal stop reasons. |
| `CORR-UPLOAD-006` | Automated + manual | `CasterUploadProfileTest`; runtime mapping review | Implemented, not field-tested | Validate RTK2go host detection forces safety status and non-user-disableable enforced rules. |
| `CORR-UPLOAD-007` | Automated + manual | `SessionWritersTest`; `SessionMetadataTest`; `CasterUploadEventJsonTest`; redacted event/output review | Implemented, not field-tested | Validate upload events and metadata redact credentials while preserving runtime state and profile identity. |
| `CORR-UPLOAD-008` | Automated + manual | `NtripCasterUploadClientTest`; BKG caster source upload smoke test | Implemented, not field-tested | Validate NTRIP v1 source upload emits classic `SOURCE <password> /mount` syntax, rejects unsafe mountpoints and waits for `ICY 200 OK` before streaming RTCM. |
| `CORR-UPLOAD-009` | Automated + manual | `NtripCasterUploadClientTest`; v2 caster source upload smoke test | Implemented, not field-tested | Validate NTRIP v2 source upload emits HTTP `POST /mount HTTP/1.1`, uses chunked transfer framing, rejects unsafe mountpoints and waits for HTTP 200 before streaming RTCM. |
| `PRODUCT-NONGOAL-001` | Review | Dependency and UI review | Passing | No maps/GIS/shapefile scope is allowed. |
| `PRODUCT-USB-001` | Review | User-facing copy and UI label review | Needs review | Android internal GNSS is not implied as a receiver input. |
| `SATMON-ARCH-001` | Automated + review | `SatelliteMonitorAggregatorTest`; `SatelliteMonitorControllerTest`; service advisory-parser call-site review | Implemented, not field-tested | Aggregation is wired from advisory UM980/u-blox/RTCM/RTKLIB telemetry after raw writes. Hardware and replay validation remain required. |
| `SATMON-STATE-001` | Automated + manual | `SatelliteMonitorAggregatorTest`; `SatelliteMonitorControllerTest`; `DashboardServiceMapperTest`; supported receiver/caster smoke tests later | Implemented, not field-tested | Distinct R/B/S freshness, unavailable state, partial-frequency diagnostic and populated bar payloads are implemented. |
| `SATMON-PROFILE-001` | Automated + manual | `ProfileStoresTest`; `ProfileDefaultsTest`; service dashboard message review | Implemented, not field-tested | Command profiles carry explicit satellite telemetry capability. Built-in UM980 and M8T profiles declare telemetry support and enable monitor-ready messages where applicable. |
| `WF-ROVER-001` | Automated + manual | Workflow validator tests; plain rover smoke test | Needs review | Confirm no NTRIP connection opens. |
| `WF-ROVER-NTRIP-001` | Automated + manual | NTRIP tests; EUREF/CORS field session | Needs review | Confirm RTK float/fix with correction recording. |
| `WF-ROVER-RTKLIB-001` | Automated + manual | Workflow classification tests; active recording configuration tests; M8T RTKLIB field session | In progress | Rover + RTKLIB uses Android-side NTRIP as correction input to RTKLIB without requiring NTRIP-to-receiver. Further M8T field validation remains useful. |
| `WF-TEMPBASE-001` | Automated + manual | `CoordinateAveragingControllerTest`; `DashboardStateTest`; temporary-base field test | Implemented, not field-tested | Temporary-base averaging/acceptance is wired through dashboard candidates and accepted-coordinate persistence. Real UM980 workflow field validation remains needed. |
| `WF-TEMPBASE-002` | Automated + manual | `CoordinateAveragingControllerTest`; temporary-base mountpoint-switch smoke test | Implemented, not field-tested | Active averaging explicitly survives accepted NTRIP caster/mountpoint changes; field validation with multiple correction bases remains needed. |
| `WF-TEMPBASE-HEIGHT-001` | Automated + review | `AcceptedBaseCoordinateTest`; `OnlineCoordinateAveragerTest`; `CoordinateAveragingControllerTest`; `RecordingServiceStateTest`; `DashboardStateTest`; UM980 fixed-base command review | Implemented, not field-tested | Current and averaged base candidates preserve ellipsoidal height, MSL altitude and derived geoid separation separately, without reusing stale display height values; UM980 fixed-base command generation uses MSL altitude only. |
| `WF-FIXEDBASE-001` | Automated + manual | Workflow validator; `DashboardStateTest`; fixed-base coordinate display/start field test | Implemented, not field-tested | Accepted-coordinate persistence and fixed-base candidate display are implemented; start behavior still needs real hardware validation. |
| `WF-FIXEDBASE-002` | Automated + review | Active recording config tests | Passing | Fixed-base start rejects `MODE ROVER` command profiles. |
| `WF-FIXEDBASE-003` | Automated + review | `FixedBaseCommandValidatorTest`; fixed-base start preflight review | Implemented, not field-tested | Fixed-base materialization/start are restricted to UM980/Unicore command profiles, and the visible `MODE BASE <lat> <lon> <msl-altitude>` line must match the selected accepted coordinate. Fresh Android app unit-test execution still needs a host where the Android unit-test task can complete cleanly. |
| `WF-FIXEDBASE-004` | Automated + manual | `FixedBaseCommandProfileSelectionTest`; `FixedBaseProfileMaterializerTest`; `FixedBaseCommandValidatorTest`; `FixedBaseHandoffPlannerTest`; dashboard Base handoff and manual UM980 `tx-to-receiver.raw` review | Implemented, not field-tested | Base handoff asks for coordinate source, selects a `MODE BASE` fixed-base settings set, derives immutable sets/profiles, applies changes only after final OK, and preserves RTCM output lines. |
| `WF-BUILTIN-SETTINGS-001` | Automated + manual | `BuiltInSettingsSetTest`; first-launch settings-set inspection | Implemented, not field-tested | Protected UM980 plain-rover, rover+NTRIP, temporary-base and fixed-base settings sets exist; NTRIP source upload is off by default. |
| `WF-RTKLIB-001` | Automated + review | RTKLIB input route tests; RTKLIB server-backend source-structure tests; RTKLIB profile frequency-count tests; `RtklibSolutionCandidateMapperTest`; M8T field replay using `samples/debug/session-2026-06-21T17-08-13...zip`; M8T outage session `samples/debug/session-2026-06-22T21-21-29...zip`; Android Studio/CI native `.so` build | In progress | Routing/API foundation, bounded worker and JNI/native bridge are implemented. Source-structure tests now require RTKLIB `rtksvr_t` memory-buffer processing. The 2026-06-22 M8T outage session shows USB and NTRIP recovery events with RTKLIB status continuing and zero dropped rover/correction bytes, but also exposed duplicated RTKLIB NMEA/POS text output. Native `.so` build still needs validation on a host with a working Android NDK. |
| `WF-SOLUTION-001` | Automated + review | Best-solution selector tests; recording tick policy tests; `RtklibSolutionCandidateMapperTest`; session metadata tests | Passing | Screen and mock-location source policies can be selected separately. RTKLIB snapshots with coordinates are mapped into `SolutionCandidate(engine=RTKLIB_REALTIME)`. |
| `RX-UM980-001` | Automated + manual | UM980 parser tests; binary session replay | Needs review | Mixed-stream parser coverage should keep expanding. |
| `RX-UM980-RTK-001` | Automated + manual | BESTNAV/ADRNAV/RTKSTATUS/RTCMSTATUS tests; field session | Needs review | Real receiver/caster validation remains important. |
| `RX-UM980-PPP-001` | Automated + manual | PPPNAV parser/dashboard tests; debug sessions | Passing | Local tests cover label mapping; field regression should continue. |
| `RX-UM980-PROFILES-001` | Automated + review | Profile store migration/protected-profile tests; built-in satellite-telemetry default tests | Passing | Built-in profiles are protected, copyable and declare satellite telemetry capability. |
| `RX-UM980-RTKLIB-001` | Automated + review | RTKLIB input route tests; UM980 binary parser tests; UM980 sample-readiness tooling | Implemented, not field-tested | OBSVMB is direct. OBSVMCMPB is routed through the named `rtkcollector-obsvmcmp-shim`; monitor parsing for OBSVMB/OBSVMCMPB/BESTSATB exists. UM980 parser tests cover BESTSATB Satellite System enum handling, split Satellite ID fallback and GLONASS PRN normalisation; replay with actual OBSVMCMPB and BESTSATB frames covers monitor-key normalisation across GPS, GLONASS, Galileo and BeiDou. Broader receiver/caster replay validation remains open. |
| `RX-UBLOX-M8T-001` | Automated + hardware | u-blox RAWX/NAV-SAT parser tests; M8T built-in profile tests; M8T device test | Implemented, not field-tested | RAWX rover visibility and NAV-SAT selected-satellite usage parsing exist. M8T command profiles are protected built-ins and declare u-blox telemetry capability. |
| `RX-UBLOX-RTKLIB-001` | Automated + hardware | RTKLIB input route tests; RTKLIB profile frequency-count tests; RTKLIB satellite-usage bridge tests; M8T/M8P replay test later | Implemented, not field-tested | UBX RAWX/SFRBX route is modelled as direct RTKLIB-EX input; RTKLIB snapshots expose bounded per-satellite usage for monitor bars. Native `.so` and fresh replay/field validation remain open. |
| `SESSION-FILES-001` | Automated + manual | Session writer tests; completed sessions | Needs review | Keep artifacts separate by source and purpose. |
| `SESSION-META-001` | Automated + review | Session metadata tests; session format review | Passing | Metadata includes workflow and solution-policy context and must exclude secrets. |
| `SESSION-NMEA-001` | Automated + sample replay | NMEA exporter/re-exporter tests; `SessionNmeaExporterTest`; `ReceiverNmeaReexporterTest`; high-rate samples | Needs review | Verify sub-second UTC preservation, source-aware share naming and receiver-only regeneration. |
| `SESSION-NMEA-002` | Automated + manual | `SafNmeaReplacementTest`; SAF replacement-state review; provider fallback smoke test | Implemented, not field-tested | Existing NMEA remains authoritative until a non-empty replacement is verified, with rollback on replacement/provider failure; common Android providers still need validation. |
| `SESSION-RTCM-001` | Automated + manual | Correction-input raw/RTCM3 tests; downstream replay | Needs review | Ensure output works with external pipelines. |
| `SESSION-RTKLIB-001` | Automated + manual | RTKLIB output writer tests; RTKLIB synthetic-output tests; cumulative native-output bridge test; `RtklibSessionPostprocessorTest`; `SessionNmeaExporterTest`; `tools/test_rtklib_native_build_files.py`; `tools/rtklib_replay_session_check.py`; RTKLIB-enabled session later | In progress | NMEA/POS artifact model and native output bridge exist. If native text output is missing while a valid snapshot exists, Kotlin synthesizes minimal RTKLIB NMEA/POS lines. Source-aware NMEA sharing keeps RTKLIB NMEA separate from receiver NMEA. Completed filesystem sessions can explicitly regenerate forward or forward/backward postprocessed artifacts through the native RTKLIB `convrnx()`/`postpos()` path, using converted base RINEX header reference coordinates. Local M8T session replay showed valid Q=1/Q=2 console `postpos()` output with that reference source. Host native build and fresh field/device validation remain open. |
| `SESSION-ARCHIVE-001` | Automated + manual | `ActiveRecordingSessionRegistryTest`; `SessionArchiveManagerTest`; Activity recreation smoke test | Implemented, not field-tested | Destructive operations reject the service-owned session independently of cached UI state; Activity recreation and Android lifecycle behaviour still need smoke testing. |
| `SESSION-ARCHIVE-004` | Automated + review | `ActiveRecordingSessionRegistryTest`; `FilesystemSessionBrowserTest`; `SafSessionActionsSourceTest` | Implemented, not field-tested | Activation and destructive archive/delete operations share an atomic registry lease; Android provider and Activity-race smoke testing remain open. |
| `SESSION-SHARE-001` | Automated + manual | `SessionArchiveManagerTest`; `SessionNmeaExporterTest`; `SafSessionActionsSourceTest`; active-session share smoke test | Implemented, not field-tested | Filesystem and SAF temporary ZIP/NMEA share entry points recheck the service-owned active-session registry at execution and clean failed cache outputs; Android background timing remains untested. |
| `SESSION-ARCHIVE-002` | Automated + manual | `SessionArchiveManagerTest`; archive/source and restore/content verification review; SAF provider failure smoke test | Implemented, not field-tested | Authoritative source deletion occurs only after destination content is reopened and verified; provider failure and real recorded-session validation remain open. |
| `SESSION-ARCHIVE-003` | Automated + review | `ArchiveIntegrityTest`; `SessionArchiveManagerTest`; filesystem and SAF restore review | Implemented, not field-tested | Restore is path-safe and bounded against traversal, duplicates, excessive expansion and provider operations; Android provider behaviour remains untested. |
| `ANDROID-SERVICE-001` | Review + manual | Service ownership review; background recording smoke test | Needs review | Screen-off and background testing remain required. |
| `ANDROID-CAPTURE-001` | Review + manual | Capture code review; background/minimised session | Needs review | UI/parser failures must remain advisory. |
| `ANDROID-DIAGNOSTICS-001` | Automated + review | Diagnostics controller tests and service call-site review | In progress | Runtime logging and performance monitoring are guarded and advisory. Field performance validation remains useful. |
| `ANDROID-JNI-001` | Automated + review + manual | JNI/native bridge source-structure tests; native call-site review; Android Studio/CI native build and device exercise | In progress | Native RTKLIB bridge now has source tests for JNI input guards and `convrnx()` non-null output strings. Full native `.so` behaviour still needs host/device validation. |
| `ANDROID-USB-001` | Automated + device | USB decision tests; Huawei P30 Pro smoke test | Needs field retest | Vendor Android edge case. |
| `ANDROID-USB-002` | Automated + manual | Reconnect policy tests; disconnect/reconnect smoke test | Needs review | Must preserve existing session artifacts. |
| `ANDROID-USB-003` | Automated + manual | Recording health monitor tests; USB silence/reconnect field test | Needs field validation | Empty USB reads are degraded stalls, not healthy recording. |
| `ANDROID-USB-004` | Automated + manual | Recording health monitor protocol-stall tests; UM980 garbage-bytes session regression | Needs field validation | Detect bytes-without-valid-protocol-frame and reconnect without stopping raw capture. |
| `ANDROID-CORRECTION-001` | Automated + manual | Recording health monitor tests; NTRIP EOF reconnect test; network interruption smoke test | Needs field validation | Correction stalls reconnect without stopping receiver raw capture. |
| `ANDROID-CORRECTION-002` | Automated + manual | Dashboard service mapper test; rover + NTRIP dashboard smoke test | Needs field validation | Correction last-update display uses wall-clock UTC, not elapsed realtime. |
| `ANDROID-CORRECTION-003` | Automated + review | `NtripRuntimeControllerTest` callback-failure coverage; runtime review | Passing | Callback exceptions publish an explicit terminal network-error state rather than leave `STREAMING`, while raw recording remains active. |
| `ANDROID-CORRECTION-004` | Automated + review | `NtripRuntimeControllerTest` blocked-callback/stubborn-client tests; service replacement/finalisation review | Implemented, not field-tested | NTRIP callback deactivation and worker join are bounded; false replacement results publish correction-scoped `RECONNECT_WAIT` state and retry under an owned coordinator; replacement startup is atomic with stop. Unconfirmed workers remain owned for retry before writer close. Real-caster cancellation timing remains untested. |
| `ANDROID-UPLOAD-001` | Automated + manual | Service call-site review; upload-outage recording smoke test | Implemented, not field-tested | Upload starts downstream of raw capture and failure remains advisory. |
| `ANDROID-UPLOAD-002` | Automated + review | Caster upload profile validation and retry-schedule tests | Implemented, not field-tested | Fixed/adaptive retry bounds enforce the ten-second minimum. |
| `ANDROID-UPLOAD-003` | Automated + manual | Upload controller watchdog/safety-stop tests; offline/high-rate smoke test | Implemented, not field-tested | Terminal safety reasons remain visible without stopping receiver capture. |
| `ANDROID-UPLOAD-004` | Automated + review | RTK2go host mapping and forced-safety tests | Implemented, not field-tested | RTK2go safety is forced and distinguishable from user-selected policy. |
| `ANDROID-UPLOAD-005` | Automated + manual | Upload snapshot/dashboard mapper tests; detailed monitor smoke test | Implemented, not field-tested | Telemetry is based on RTCM frames actually offered for upload. |
| `ANDROID-STORAGE-001` | Manual + review | SAF folder-picker storage profile and service start review | Needs provider testing | Verify persisted write permission on common Android document providers. |
| `ANDROID-STATE-001` | Automated + review | `AndroidStateReceiverRegistrationTest`; private receiver source review; cross-UID smoke test | Implemented, not field-tested | Recording state and USB permission callbacks are non-exported on every supported API; cross-UID and supported-API device validation remain open. |
| `ANDROID-MOCK-001` | Automated + manual | Mock-location mapper tests; mock altitude smoke test | Needs review | `Location.altitude` must use ellipsoidal height. |
| `ANDROID-MOCK-002` | Review | Android API limitation review; user-doc wording | Passing | Satellite counts may be extras; full `GnssStatus` injection is not promised. |
| `ANDROID-MOCK-003` | Automated + manual | Mock publish tick/dashboard tests; RTKLIB-only mock policy tests; mock provider field run | Needs review | Monitor status, last publish interval, selectable fixed rate and effective rate. RTKLIB-only policy must publish only fresh RTKLIB candidates and must not fall back to receiver-internal fixes. |
| `ANDROID-MOCK-004` | Automated + manual | Mock-location mapper tests; Android sink review; device mock-provider smoke test | Needs review | Publishes all supported public `Location` fields available from the selected best solution. |
| `SEC-SECRETS-001` | Automated + review | Session/settings export tests | Needs review | Include plaintext password export option. |
| `SEC-SETTINGS-001` | Automated + manual | Settings export tests; export UI review | Needs review | Plaintext passwords must be opt-in. |
| `SEC-IMPORT-001` | Automated + manual | Settings import validation tests; Android JSON open flow | Needs review | Reject malformed or unsafe imports. |
| `SEC-IMPORT-002` | Automated + manual | `SettingsImportModelsTest`; complete profile restore and effective SAF grant sanitisation review; second-device import smoke test | Implemented, not field-tested | Imported URI text in profiles or inherited-kind overrides never grants SAF authority; unsafe override URIs are stripped so a locally reselected profile can supply the grant. Second-device validation remains open. |
| `SEC-IMPORT-003` | Automated + manual | `SettingsImportModelsTest`; `SharedPreferencesTransactionsTest`; profile and secret-store transaction review; import outcome UI review | Implemented, not field-tested | Profile and batch-secret commits synchronously restore exact prior touched values after `commit(false)`; imported credentials remain disarmed until the separate secret transaction succeeds. Android Keystore/provider failure behaviour remains untested. |
| `SEC-IMPORT-004` | Automated + manual | `SettingsImportModelsTest`; `NtripSecretStore` import call-site review; credential-collision and omitted-family smoke tests | Implemented, not field-tested | Represented profiles and imported override credentials are re-keyed without deleting local secrets; omitted optional families retain their local profiles and credentials, while imported endpoint overrides are disarmed. Cross-device and Keystore validation remain open. |
| `SEC-IMPORT-005` | Automated + manual | `SettingsImportModelsTest` present optional-family type tests; import UI review | Implemented, not field-tested | Only absent optional-family keys are historical omissions; present malformed values reject, while present empty arrays are intentional empty families. Android share/import smoke testing remains open. |
| `SEC-DIAGNOSTICS-001` | Automated + review | Diagnostics redaction and disabled-state tests | In progress | Diagnostics sharing uses app-private logs and scoped temporary ZIPs. |
| `PLAY-DISCLOSURE-001` | Review + manual | Privacy policy source; Play publication checklist source; Play Console answer review | Implemented, not field-tested | `PRIVACY.md` and `docs/play-publication.md` exist as source documents; published policy URL and Play Console answer review remain required. |
| `PLAY-DATA-001` | Review + manual | NTRIP transport/source-upload code review; Data safety checklist | Implemented, not field-tested | Privacy, Play checklist and NTRIP profile editors disclose that cleartext TCP may expose credentials, GGA and source-upload data; Play Console Data safety review remains required. |
| `PLAY-PERM-001` | Automated + review | Manifest lint/tests; release manifest review | Implemented, not field-tested | Release manifest keeps mock-location debug-only and declares recording notification/foreground-service permissions; Play pre-upload checks remain required. |
| `PLAY-NOTIFICATION-001` | Automated + manual | Notification permission policy tests; Android 13+ denial/acceptance smoke test | Implemented, not field-tested | Notification permission model and start gating exist; Android 13+ denial/acceptance smoke test remains required. |
| `PLAY-FGS-001` | Review + manual | Manifest service type review; Play foreground-service declaration | Implemented, not field-tested | Recording service declares `connectedDevice|dataSync` and Play rationale is documented; release Play declaration review remains required. |
| `PLAY-SECRETS-001` | Automated + manual | Settings backup policy/share tests and delayed cache cleanup scheduling in `MainActivity` | Implemented, not field-tested | Cleanup is best-effort and limited to matching backup-cache filenames; manual cache-inspection validation is not yet available in this change. |
| `PLAY-IMPORT-001` | Automated + manual | Settings import validator, intent-reader, active-session registry and manifest tests; Android Files/share smoke test | Implemented, not field-tested | Import uses explicit content/share intents, rejects file URI imports, previews/validates JSON and rechecks service-owned recording state at the write boundary; Android Files/share smoke test remains required. |
| `PLAY-BUILD-001` | Automated + manual | `validateGooglePlayReleaseBuildInputs`; `validateGooglePlayReleaseBundle`; release AAB build | Implemented, not field-tested | Gradle release checks exist; full release AAB build still needs Windows Android Studio or CI validation. |
| `PLAY-LICENSE-001` | Review + manual | Gradle dependency report; `docs/third-party-licenses.md` | Implemented, not field-tested | Third-party runtime, build/test and bundled RTKLIB-EX licence inventory exists; final release review must confirm no dependency drift. |
| `UI-DASH-001` | Manual + review | Phone/tablet portrait/landscape screenshots | Needs review | Visual fidelity and nav-bar checks. |
| `UI-DASH-002` | Manual + review | Live UM980 session screenshots/video | Needs review | No jumping cards. |
| `UI-DASH-003` | Manual + review | Phone portrait dashboard screenshot | Needs review | Live-monitoring cards should switch to single column when needed. |
| `UI-DASH-004` | Automated + manual | Dashboard state/action tests; dashboard chip smoke test | Needs review | Mock GPS chip is top-level; no placeholder Mark action. |
| `UI-DASH-005` | Automated + manual | Dashboard formatter/layout tests; dashboard screenshot review | Needs review | Dashboard labels and dynamic/static distance units are consistent across cards. |
| `UI-DASH-006` | Review + manual | `HomeDashboard` alert placement review; battery-optimisation recording smoke test | Implemented, not field-tested | Reliability warnings render after compact setup controls or above rail monitoring cards and no longer displace the app title. |
| `UI-SATMON-001` | Automated + manual | `SatelliteMonitorDashboardModelsTest`; `SatelliteMonitorControllerTest`; dashboard screenshot review | Implemented, not field-tested | Main card displays the selected main engine and has no main-card engine selector. |
| `UI-SATMON-002` | Automated + manual | `SatelliteMonitorDashboardModelsTest`; dashboard screenshot review | Implemented, not field-tested | Main card groups constellation first, then frequency. |
| `UI-SATMON-003` | Automated + manual | `SatelliteMonitorDashboardModelsTest`; dashboard screenshot review | Implemented, not field-tested | Boxed bars encode used as saturated prefix inside visible total. |
| `UI-SATMON-004` | Manual | Dashboard help review | Needs review | R/B/S labels and info affordance are present in UI; screenshot review remains open. |
| `UI-SATMON-005` | Automated + manual | `DashboardLayoutModelsTest`; dashboard screenshot review | Implemented, not field-tested | Light default and satellite-card dark switch are modelled. |
| `UI-SATMON-006` | Automated + manual | `SatelliteMonitorDashboardModelsTest`; detailed screen screenshot review | Implemented, not field-tested | Detailed grouping model supports frequency-primary and constellation-primary modes. |
| `UI-BASE-001` | Automated + manual | Dashboard state tests; temporary-base dashboard review | Needs review | Keep controls compact and explicit. |
| `UI-UPLOAD-001` | Automated + manual | Dashboard upload-card visibility/model tests; portrait/landscape screenshots | Implemented, not field-tested | Enabled upload remains visible before and during recording. |
| `UI-UPLOAD-002` | Automated + manual | Upload dashboard mapper/detail-screen tests; live monitor review | Implemented, not field-tested | State, errors, retries, volume, bitrate, RTCM rates and safety status are surfaced. |
| `UI-SETUP-001` | Automated + manual | Active setup resolver tests; settings hub review | In progress | Active settings set is visible in the settings hub. Main-menu refinement remains under review. |
| `UI-SETUP-002` | Automated + manual | Active setup resolver tests; settings-set editor review | In progress | Policy model and JSON persistence exist; compact editor polish remains. |
| `UI-SETUP-003` | Automated + manual | `DashboardUploadSelectorTest`; `DashboardLayoutModelsTest`; dashboard selector smoke test | Implemented, not field-tested | Main setup strip includes Upload, and Off is a real first-row source-upload disabled state. |
| `UI-SETUP-004` | Automated + manual | `NtripCasterUploadClientTest`; caster upload profile editor review | Implemented, not field-tested | Source-upload v1/v2 protocol paths are separate; manual UI review should confirm username is visibly unused but preserved for v1. |
| `UI-SETUP-005` | Automated + manual | `DashboardStateTest`; `DashboardLayoutModelsTest`; Settings hub review; Settings Device selector smoke test | Implemented, not field-tested | Device is first in the Home setup strip, Settings exposes Device as a button-like control, and selecting Device from Settings keeps the user in Settings. |
| `UI-SETUP-006` | Automated + manual | `DashboardStateTest`; `ProfileListModelsTest`; dashboard/settings smoke test | Implemented, not field-tested | Active out-of-filter settings sets and init/shutdown profiles remain visible and are warning-highlighted until a compatible item is selected. |
| `WF-PROFILE-FILTER-001` | Automated + manual | `ProfileDeviceFilterTest`; profile-selector smoke test; device-console smoke test | Implemented, not field-tested | Persistent Device selector filters settings sets and init/shutdown profiles while preserving Any for broad profile access. |
| `WF-UPLOAD-UI-001` | Automated + manual | `DashboardStateTest`; dashboard smoke test | Implemented, not field-tested | Upload tile is disabled/not needed for rover workflows and remains selectable for temporary/fixed base workflows. |
| `UI-PROFILE-002` | Automated + manual | Profile compatibility tests; profile editor smoke test; long profile selector smoke test | In progress | Compatibility model and controlled RTKLIB/solution references exist; profile selector dialogs are scrollable. Grouping/reorder UI remains open. |
| `UI-PROFILE-001` | Automated + manual | Protected-profile tests; profile editor smoke test | Needs review | Built-ins are view-only and copyable. |
| `UI-KEYBOARD-001` | Manual + review | Hardware keyboard command-editor smoke test | Needs field retest | Arrow keys stay inside native multiline editor; Tab/Shift+Tab traverse fields. |
| `UI-KEYBOARD-002` | Manual + review | Profile editor compile coverage; profile/settings one-line editor smoke test | Implemented, not field-tested | Editable one-line profile/settings fields now use the shared native Android-backed field. Hardware keyboard smoke testing remains needed. |
| `UI-STORAGE-001` | Manual + review | Storage profile folder-picker smoke test | Needs provider testing | Tree URI is picker-selected and display-only. |
| `UI-DOCS-001` | Review | Docs review | Passing | Formal specs and user docs are separate. |
